# Raw archive placeholder

Before registration, place the exact source files named by every
`manifest.json` `sources[].original_filename` entry in this directory and
replace the placeholder SHA-256 values. Do not commit licensed archives unless
their redistribution terms explicitly permit it.

This README is documentation only; it is not accepted as an official source
archive and the template bundle remains intentionally non-registerable.
